# Homework 6 - AI-Powered Multi-Agent Banking Pipeline (Node.js) - Detailed Build Plan

## How to use this document

This document is the build plan. Implementation happens in a separate Claude Code session.

## Context (read first - the word "agent" means two different things)

1. **Four meta-agents** = the AI build _workflows_ (the graded deliverables): Agent 1 Specification, Agent 2 Code-generation, Agent 3 Unit-tests, Agent 4 Documentation. Realized as slash commands + a coverage hook + the context7 workflow.
2. **Pipeline runtime agents** = the _system_ those meta-agents produce. Task 2 requires at least 3 cooperating modules. We build the minimum: Transaction Validator -> Fraud Detector -> Reporting Agent.

`homework-6/` is greenfield (only `TASKS.md` + `sample-transactions.json`). The `specification-TEMPLATE-hint.md` and starter `agents.md` referenced by the task are missing, so Task 1 creates them, using `../homework-3/specification-TEMPLATE-example.md` (Banking-Specific template) as the reference.

## Global decisions (adjust on review)

| Choice              | Value                                                                            |
| ------------------- | -------------------------------------------------------------------------------- |
| Language            | Node.js 22, ESM (`"type": "module"`)                                             |
| Money               | `decimal.js` - never float; `ROUND_HALF_UP` for rounding                         |
| Currency            | ISO 4217 validation via the `currency-codes` package                             |
| UUIDs               | built-in `node:crypto` `randomUUID()`                                            |
| Tests/coverage      | `vitest` + `@vitest/coverage-v8`, `json-summary` reporter; gate 80%, aim >= 90%  |
| MCP server          | `@modelcontextprotocol/sdk` (+ `zod`), stdio transport                           |
| Author              | Alona Holovko                                                                    |
| Commands dir        | project-local `.claude/commands/`                                                |
| Spec origin         | generated by running `/write-spec`, then refined (makes Agent 1 a real workflow) |
| `/write-spec` scope | generic + reusable (takes a feature description; reads the hint template)        |

## Validation + fraud rules (derived from `sample-transactions.json` - exact, so the implementation session is deterministic)

**Validator - reject if any:** missing required field; `currency` not in ISO 4217; `amount` not a numeric string matching `^-?\d+(\.\d{1,2})?$`; `amount` <= 0 **unless** `transaction_type === "refund"` (refunds may be negative).

**Fraud score** (additive, capped at 100; flag when score >= 50):

| Signal          | Condition                                                    | Points |
| --------------- | ------------------------------------------------------------ | ------ |
| High-value      | `amount` > 10,000                                            | +50    |
| Off-hours       | UTC hour of `timestamp` in 0-5                               | +25    |
| Cross-border    | `metadata.country !== "US"` OR `currency !== "USD"`          | +25    |
| Wire high-value | `transaction_type === "wire_transfer"` AND `amount` > 10,000 | +15    |

**Expected outcome on the 8 samples** (this is the acceptance oracle):

| Txn                              | Why                                          | Result                             |
| -------------------------------- | -------------------------------------------- | ---------------------------------- |
| TXN001 1500 USD transfer         | clean                                        | validated, not flagged             |
| TXN002 25000 USD wire            | 50+15=65                                     | flagged                            |
| TXN003 9999.99 USD transfer      | just under 10k -> 0                          | validated, not flagged (edge case) |
| TXN004 500 EUR transfer 02:47 DE | 25+25=50                                     | flagged                            |
| TXN005 75000 USD wire            | 50+15=65                                     | flagged                            |
| TXN006 200 XYZ                   | bad currency                                 | rejected (never reaches fraud)     |
| TXN007 -100 GBP refund GB        | negative allowed for refund; cross-border 25 | validated, not flagged             |
| TXN008 3200 USD transfer         | clean                                        | validated, not flagged             |

Net: 7 validated, 1 rejected, 3 flagged.

## Target file tree

```
package.json  vitest.config.js
specification.md  specification-TEMPLATE-hint.md  agents.md
research-notes.md  mcp.json  README.md  HOWTORUN.md
sample-transactions.json            (exists)
integrator.js
agents/{transaction_validator,fraud_detector,reporting_agent}.js
agents/lib/{messaging,money,logger}.js
mcp/server.js
scripts/{check-coverage.cjs,pre-push-guard.cjs}
shared/{input,processing,output,results}/.gitkeep
tests/{lib,agents,integration}/*.test.js
.claude/settings.json
.claude/commands/{write-spec,run-pipeline,validate-transactions}.md
docs/screenshots/   (5 PNGs - manual capture)
```

## Message envelope (standard, all stages)

```json
{
  "message_id": "uuid4",
  "timestamp": "ISO-8601Z",
  "source_agent": "...",
  "target_agent": "...",
  "message_type": "transaction",
  "data": {
    "transaction_id": "TXN001",
    "amount": "1500.00",
    "currency": "USD",
    "status": "validated"
  }
}
```

Flow: `input/` -> validator -> `processing/` -> `output/` -> fraud -> `output/` -> reporting -> `results/`.

---

## Task 1 - Agent 1: Specification (Required)

**Goal:** the spec + project context + a slash command that generates specs.

**Status: DONE** - committed (`4aa0359` spec, `5264152` screenshots).

### Deliverables

- [x] `specification-TEMPLATE-hint.md`
- [x] `.claude/commands/write-spec.md`
- [x] `specification.md`
- [x] `agents.md`

### Build steps

- [x] Create `specification-TEMPLATE-hint.md` - banking spec template adapted from HW3, with the 5 sections and the Low-Level Task block in TASKS.md format: `Task: [Agent] / Prompt: "..." / File to CREATE / Function to CREATE / Details`.
- [x] Create `.claude/commands/write-spec.md` - reusable slash command: reads the hint template + `agents.md`, takes a feature description via `$ARGUMENTS` (asks if absent), emits a spec with all 5 sections, enforcing testable mid-level objectives and one Low-Level Task per agent.
- [x] Run `/write-spec` for the banking pipeline -> produce `specification.md`; refine it (5 sections; one Low-Level Task per pipeline agent + integrator).
- [x] Create `agents.md` - project AI-context: overview, stack/conventions, message protocol + `shared/` layout, the 3 agent roles, logging/PII rules, npm scripts.

### Acceptance

- [x] `specification.md` has all 5 sections + a Low-Level Task per agent
- [x] `/write-spec` generates a spec from the template
- [x] `agents.md` present and accurate

---

## Task 2 - Agent 2: Build the Pipeline (Required, uses context7)

**Goal:** 3 cooperating agents passing JSON through `shared/`, processing all 8 samples.

**Status: DONE & verified** - committed (`70dfb05`); run matched the oracle (7 validated / 1 rejected / 3 flagged). Switched currency validation to `currency-codes`; fixed a `process()`-shadows-global-`process` crash.

### Deliverables

- [x] `package.json`
- [x] `agents/lib/messaging.js`, `agents/lib/money.js`, `agents/lib/logger.js`
- [x] `agents/transaction_validator.js`, `agents/fraud_detector.js`, `agents/reporting_agent.js`
- [x] `integrator.js`
- [x] `research-notes.md`
- [x] `shared/**/.gitkeep`

### Build steps

- [x] `package.json` (ESM) + install `decimal.js currency-codes @modelcontextprotocol/sdk zod` and dev `vitest @vitest/coverage-v8`. Scripts: `pipeline` -> `node integrator.js`; `validate` -> `node agents/transaction_validator.js --dry-run`; `test` -> `vitest run`; `coverage` -> `vitest run --coverage`.
- [x] `lib/messaging.js`: `createMessage({source_agent,target_agent,message_type,data})` (randomUUID + ISO timestamp), `writeMessage(dir,msg)`, `readMessages(dir)`, `moveMessage(file,fromDir,toDir)`, `ensureDirs()`, `isoNow()`.
- [x] `lib/money.js`: decimal.js helpers - `toDecimal(str)`, `isValidAmountString(str)`, `gt(a,b)`, `round2(d)` (ROUND_HALF_UP).
- [x] `lib/logger.js`: `audit({agent,transactionId,outcome,detail})` -> ISO-timestamped line; `redact(value)` masks account numbers/names.
- [x] `transaction_validator.js`: `process(message)` applies the Validator rules; `run()` does file I/O; `--dry-run` validates sample-transactions.json and prints a table, no file moves.
- [x] `fraud_detector.js`: `process(message)` computes `risk_score` + `flagged` per the fraud table; passes downstream.
- [x] `reporting_agent.js`: aggregates into `shared/results/<txn_id>.json` + `shared/results/pipeline-summary.json`.
- [x] `integrator.js`: `ensureDirs()`, load samples, emit input messages, run Validator -> Fraud -> Reporting in order, print a console summary.
- [x] context7: run at least 2 real queries and log each in `research-notes.md` (search term, library ID, applied insight).

### Acceptance

- [x] `npm run pipeline` runs clean; all 8 txns in `shared/results/`
- [x] Outcome matches the oracle table (TXN006 rejected; TXN002/004/005 flagged)
- [x] Every file in `shared/` is a valid message envelope

---

## Task 3 - Agent 3: Skills & Coverage-Gate Hook (Required)

**Goal:** slash commands + a hook that blocks `git push` when coverage < 80%.

**Status: DONE** - 5 files built; coverage scripts hardened to CommonJS `.cjs` so the hook runs under any node Claude Code spawns; hook demonstrably blocks `git push` under 80% coverage. Demos captured (`run-pipeline.png`, `hook-trigger.png`).

### Deliverables

- [x] `.claude/commands/run-pipeline.md`
- [x] `.claude/commands/validate-transactions.md`
- [x] `scripts/check-coverage.cjs`
- [x] `scripts/pre-push-guard.cjs`
- [x] `.claude/settings.json`

### Build steps

- [x] `run-pipeline.md`: check `sample-transactions.json` -> clear `shared/` -> `npm run pipeline` -> summarize results -> list rejections + reasons.
- [x] `validate-transactions.md`: `node agents/transaction_validator.js --dry-run` -> report total/valid/invalid + reasons -> show table.
- [x] `scripts/check-coverage.cjs`: read `coverage/coverage-summary.json` -> `total.lines.pct`; print it; `process.exit(pct < 80 ? 2 : 0)`.
- [x] `scripts/pre-push-guard.cjs`: read stdin JSON; if `tool_input.command` includes `git push`, check coverage then exit 2 when `< 80%`; otherwise exit 0. (CommonJS + sync I/O so it runs under any node the hook spawns, not just Node 22.)
- [x] `.claude/settings.json`: PreToolUse hook, matcher `Bash`, command `node "$CLAUDE_PROJECT_DIR/scripts/pre-push-guard.cjs"`.

```json
{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "node \"$CLAUDE_PROJECT_DIR/scripts/pre-push-guard.cjs\""
          }
        ]
      }
    ]
  }
}
```

### Acceptance

- [x] `/run-pipeline` and `/validate-transactions` work
- [x] A `git push`-shaped Bash call is blocked when coverage < 80% (demonstrated); pass-at->=80% will hold once Task 5 tests land
- [x] A non-push Bash command is unaffected

---

## Task 4 - MCP Integration (Required, two servers)

**Goal:** context7 used in Task 2 + a custom server that makes the pipeline queryable.

**Status: DONE & verified** - `mcp/server.js` (McpServer + StdioServerTransport + zod) and `mcp.json` (context7 + pipeline-status) built; verified end-to-end with a stdio MCP client under Node 22 (both tools + the `pipeline://summary` resource respond, `get_transaction_status("TXN001")` returns `validated`, missing ids return a graceful "not found"). Both servers connect in-session (`/mcp`); `mcp-interaction` screenshots captured. Note: registered locally with explicit v22 node/npx paths to dodge the v12 nvm default; the `mcp.json` deliverable keeps plain `node`/`npx`.

### Deliverables

- [x] `mcp/server.js`
- [x] `mcp.json`
- [x] `research-notes.md` finalized (>= 2 context7 queries)

### Build steps

- [x] `mcp/server.js`: tool `get_transaction_status({ transaction_id })`; tool `list_pipeline_results()`; resource `pipeline://summary`.
- [x] `mcp.json`: `context7` (`npx -y @upstash/context7-mcp@latest`) + `pipeline-status` (`node mcp/server.js`).
- [x] Finalize `research-notes.md` (>= 2 context7 queries from Task 2).

### Acceptance

- [x] Both servers respond
- [x] `get_transaction_status("TXN001")` returns a status; `list_pipeline_results` lists all; `pipeline://summary` returns text
- [x] `research-notes.md` has 2+ queries

---

## Task 5 - Agent 4: Tests & Documentation (Required)

**Goal:** test suite meeting the gate + README/docs with the author name.

**Status: DONE & verified** - 81 tests across 8 files (all passing), coverage 97.54% lines (gate 80%, aim 90%). README + HOWTORUN written with author name, ASCII diagram, tech stack. MCP server refactored to be testable (createServer factory + exports). Integration test confirms oracle + idempotency in isolated temp dirs. All 5 screenshots captured. Ready for commit + PR.

### Deliverables

- [x] `vitest.config.js`
- [x] `tests/lib/*.test.js`, `tests/agents/*.test.js`, `tests/integration/pipeline.test.js`
- [x] `README.md`
- [x] `HOWTORUN.md`

### Build steps

- [x] `vitest.config.js`: coverage provider `v8`, reporters `['text','json-summary']`, `reportsDirectory: 'coverage'`, thresholds `lines: 80`; include `agents/**`, `mcp/**`, `integrator.js`.
- [x] `tests/lib/{money,messaging}.test.js` (plus logger.test.js).
- [x] `tests/agents/transaction_validator.test.js`: valid; missing field; bad currency (XYZ); negative non-refund -> reject; negative refund -> ok; zero -> reject.
- [x] `tests/agents/fraud_detector.test.js`: high-value flag; off-hours+cross-border flag (TXN004); just-under-threshold no flag (TXN003); clean no flag.
- [x] `tests/agents/reporting_agent.test.js`: aggregation + summary counts.
- [x] `tests/agents/mcp_server.test.js`: pure helpers (listResultIds, readResult) + MCP handlers over in-memory transport.
- [x] `tests/integration/pipeline.test.js`: full pipeline in temp dir; idempotency test; asserts oracle.
- [x] `README.md`: "Student Name: Alona Holovko", overview, one bullet per agent, ASCII architecture diagram, tech-stack table.
- [x] `HOWTORUN.md`: numbered steps 1–7 from npm install through pipeline, tests, slash commands, hook, and MCP.

### Acceptance

- [x] Coverage 97.54% (gate 80%, aim 90% — both cleared)
- [x] README has the author name (Alona Holovko) + ASCII diagram + tech table
- [x] HOWTORUN is numbered 1–7 setup->demo

---

## Screenshots & submission (manual capture)

After the implementation session builds everything, capture 5 PNGs into `docs/screenshots/` and embed them in the PR description:

- [x] `pipeline-run.png` (captured as `npm-run-pipeline.png`) - `npm run pipeline` full output
- [x] `test-coverage.png` - `npm run coverage` showing 97.54% (gate 80%, aim 90%)
- [x] `skill-run-pipeline.png` (captured as `run-pipeline.png`) - `/run-pipeline` executing
- [x] `hook-trigger.png` - coverage-gate hook blocking a push
- [x] `mcp-interaction.png` (captured as `mcp-interaction-1/2/3.png`) - a context7 query result and a custom MCP tool call
- [ ] PR description includes all five screenshots

## Notes

- `npm install` needs network. Commits/PR only when you ask.
- Reporting is the single third agent (no Compliance/Settlement); only the mandatory coverage hook.
